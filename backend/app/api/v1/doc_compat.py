"""Compatibility router placeholder.

Compatibility business endpoints have been fully migrated to formal domain routers.
This module is intentionally kept as an empty router so existing imports do not break.
"""

from fastapi import APIRouter

router = APIRouter()
